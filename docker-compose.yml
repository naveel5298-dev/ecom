version: "3.9"

services:
  app:
    build: .
    container_name: node-app
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "6379:6379"